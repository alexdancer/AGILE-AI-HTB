## 1. Add filter input and JavaScript to board template

- [x] 1.1 Add filter `<input>` above the board columns in `board.html`
- [x] 1.2 Add match count indicator `<span>` next to filter input
- [x] 1.3 Add inline `<script>` for client-side filtering logic (filter `.task` cards by `textContent`, update indicator, show "No matching tasks" in empty columns)
- [x] 1.4 Add portal test: filter hides non-matching cards
- [x] 1.5 Add portal test: empty filter restores all cards and hides indicator

## 2. Verify

- [x] 2.1 Run full pytest suite, fix any failures
